# Typed Charter intake envelope template.
# Replace every {{...}} placeholder. Keep this working file outside the target repository.
# The `content` value must be the complete object defined by
# handbook.schemas.artifacts.project-authority@1.1.0; it is YAML here and is
# normalized by the engine before immutable intake/candidate persistence.
# Canonical exception records use `.handbook/project/charter.yaml#/governance/exception_process`.

mode: guided_adaptive # guided_adaptive | express | agent_assisted
content: "{{COMPLETE_CANONICAL_CHARTER_OBJECT}}"
coverage:
  - coverage_id: project_shape.definition
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: delivery.constraints
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: delivery.default_implications
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: operational_reality.production_state
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: risk.domains
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: engineering_posture.baseline
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: policy.authority_and_revision
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: governance.decision_authority
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: governance.required_approvals
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: governance.exception_policy
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: engineering_posture.dimensions
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: engineering_posture.red_lines
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: governance.review_triggers
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: governance.reassessment_triggers
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: debt.register
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
  - coverage_id: decisions.records
    source_kind: user_declaration
    value_ref: "{{VALUE_REF}}"
    evidence_refs: []
    confidence: "{{CONFIDENCE}}"
    freshness: null
    sensitivity: "{{SENSITIVITY}}"
    contradiction_refs: []
    waiver_ref: null
consumer:
  kind: agent
  id: "{{CALLER_ID}}"
  version: "{{CALLER_VERSION}}"
prompt_event_refs: []
finalized_at_utc: "{{RFC3339_UTC}}"
expected_current_fingerprint: null # required for amendment; omit/null for initial creation
