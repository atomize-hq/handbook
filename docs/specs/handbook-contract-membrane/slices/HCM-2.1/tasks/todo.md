# HCM-2.1 Future Implementation Checklist

## Entry and authority

- [ ] Revalidate selected HCM-1.4 closeout, clean entry state, and exact HCM-1.2 Project Context definition/profile bytes.
- [ ] Reread the required live skills and exact HCM-2.1 authority sections.
- [ ] Refresh GitNexus and record upstream impact for every existing symbol before edit.
- [ ] Replay the literal 29-member package-definition manifest before code changes.

## RED proof

- [ ] Add canonical-record parse/schema/typed-decode positive and negative tests.
- [ ] Add duplicate/multi-document/non-object/unknown/missing/bounds/stable-ref/size refusal tests.
- [ ] Add the closed-emitter boundary YAML and exhaustive-transform Markdown exact-byte golden tests.
- [ ] Add distinct source/render fingerprint and semantic-equivalence domain tests.
- [ ] Add descriptor/kind/schema/path binding, retained-observation, substitution, identical-byte inode ABA, and no-follow security tests.
- [ ] Add author validate/write/overwrite/lock/no-legacy-access tests.
- [ ] Add setup and doctor `1.1.0` integration tests.
- [ ] Add flow selected-source/render/fingerprint/budget/legacy-irrelevance tests.
- [ ] Add Environment Inventory selected-YAML reference positive/refusal/no-mutation tests.
- [ ] Add native non-Unix validate-only and pre-mutation refusal tests.

## Engine canonical owner

- [ ] Land exact `CanonicalProjectContext` typed data.
- [ ] Land duplicate-safe selected-schema parse and closed decode.
- [ ] Land the exact closed canonical YAML emitter.
- [ ] Land the exhaustive plain-text transform and fixed in-memory Markdown renderer with no clock/environment input.
- [ ] Land exact source and rendered-output fingerprints.
- [ ] Land selected descriptor no-follow load.
- [ ] Retain one inspected observation through typed decode/render/fingerprint and final identity/byte stability.
- [ ] Add exact typed-decode, render, and observation-change inspection reasons.
- [ ] Convert engine identity/ingest-issue/baseline-validation paths to owned strings and prove freshness ownership adaptation preserves exact `reduced-v1-m8` preimage bytes.
- [ ] Remove retired rich-input/timestamped-renderer exports after caller cutover.

## Authoring and template cutover

- [ ] Rewire compiler authoring to selected canonical YAML and atomic single-file write.
- [ ] Preserve file/stdin and validate-only grammar with no input translator.
- [ ] Return exact path/bytes/fingerprints/media type.
- [ ] Make the Project Context author result path owned from the selected decision and remove the legacy fixed-path constant/re-exports without an alias.
- [ ] Cut the Project Context input template and live-skill fixture to canonical `1.0` YAML.
- [ ] Prove old Project Context Markdown has no authoring effect and is never written.
- [ ] Refuse non-Unix Project Context mutation before any filesystem delta while retaining validate-only.
- [ ] Cut Environment Inventory's reference-only dependency/template/fixture to the selected YAML path.

## Setup, doctor, and flow

- [ ] Prove setup missing/valid/invalid/rewrite/no-write behavior; avoid production edit if already correct.
- [ ] Add doctor schema `1.1.0` nullable Project Context fingerprint row.
- [ ] Project doctor from the retained observation and exact failure-reason precedence.
- [ ] Add only the three exact inspection-reason names to CLI setup rendering and prove all other setup behavior unchanged.
- [ ] Preserve every HCM-1.4 doctor field/status/error/JSON/exit contract.
- [ ] Install `BR-HCM-2-PILOT-FLOW-01` with selected Project Context source only.
- [ ] Project the exact owned-path DTO, `Rendered` section, source fingerprint, and rendered-output fingerprint.
- [ ] Carry descriptor paths as `String` through every named budget, flow resolver subject/next-action, compiler subject/next-action, JSON, inspect, and CLI surface with no static adapter/leak.
- [ ] Advance flow and compiler C04 result version exactly to `reduced-v1-m8.2`; accept `.2`, reject `.1`, and preserve C03 `reduced-v1-m8` generation `1`.
- [ ] Freeze source-versus-rendered budget domains, manifest identity, unchanged `reduced-v1-m8` freshness inputs without source length/rendered values, fixture lineage, logs, notes, and compiler/CLI rendering.
- [ ] Prove legacy Markdown/layout/pipeline output cannot affect selected flow truth.
- [ ] Preserve fixed sibling behavior beyond the Environment Inventory reference and preserve frozen pipeline behavior.

## Proof and closeout

- [ ] Run focused positive, negative, no-follow, observation/ABA, deterministic, DTO/budget, Environment Inventory reference, native non-Unix, and end-to-end smoke proof.
- [ ] Run HCM-1.1-HCM-1.4, author, setup, doctor, flow, CLI, workspace, Clippy, docs, Windows-target, install, and pipeline regressions.
- [ ] Replay package-definition tree and package archive by set, size, SHA-256, and bytes.
- [ ] Update only affected `00`, `03`, `04`, and `06` rows with evidence-supported classifications.
- [ ] Run all handoff validator modes, scope, secret, whitespace, diff, and GitNexus gates.
- [ ] Assemble the exact final subject manifest and immutable review dispatch.
- [ ] Obtain fresh isolated built-in review; remediate and use different fresh reviewers until CLEAN.
- [ ] Replay CLEAN bytes in staging and create the primary reviewed implementation commit.
- [ ] Create one parent v1.2 handoff, rebuild/validate ledger, and create the separate mechanical closeout commit.
- [ ] Stop without starting HCM-2.2.

## Explicitly not done in this packet

- [ ] Do not edit Project Context schema/kind/profile/descriptor definition bytes.
- [ ] Do not add migration, importer, alias, compatibility, or dual-read/write behavior.
- [ ] Do not persist derived Markdown.
- [ ] Do not change setup into an author/materializer.
- [ ] Do not change Environment Inventory authority beyond the exact selected-YAML reference-only cutover.
- [ ] Do not implement generic renderers, intake, lifecycle, semantic validation, Resolution, or Projection.
- [ ] Do not change pipeline/stage/external pipeline consumer code.
- [ ] Do not start HCM-2.2, HCM-2.3, HCM-2.4, SDK, Tauri, Substrate, contract, dock, or publication work.
