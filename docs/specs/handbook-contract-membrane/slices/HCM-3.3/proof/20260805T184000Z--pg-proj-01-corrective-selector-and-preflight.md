# HCM-3.3 PG-PROJ-01 corrective selector and preflight

**Status:** frozen planning-only corrective selector. This is a new parent
orchestration. It does not resume, amend, supersede, or reopen the completed
HCM-3.3 selector-plan parent.

## Identity and source context

- parent orchestration ID:
  `20260805T184000Z--HCM-3-3--pg-proj-01-proof-obligation-remediation`;
- packet ID:
  `HCM-3.3-P2-pg-proj-01-proof-remediation-causal-review`;
- meta workflow:
  `handbook-hcm-3-3-selector-plan-20260805`;
- bound increment thread/host:
  `019fd335-a75e-7bb0-bba9-19d5cf75c6c2` / `local`;
- dispatch nonce:
  `1539bf7ecb5544329c471a5fb8f5344b`;
- immutable source handoff:
  `20260805T172000Z--HCM-3-3--orchestration--deterministic-projection-selector-plan-completed`.

The source handoff is completed context only. Its parent orchestration,
dispatches, causal budget, findings, CLEAN result, and closeout remain
immutable. The current selector creates a distinct corrective outcome and
records no external control-plane artifact as repository authority.

## Selected corrective objective

Review and, when required, repair only the future `PG-PROJ-01` proof
obligation so it explicitly tests one byte-identical source truth, one exact
resolved profile, and one exact `ProjectionDefinition` under at least two
authorized Context Resolution envelopes. The future proof wall must establish
deterministic replay, complete rule accounting and provenance/fingerprints for
every envelope, correctly distinct or accounted outputs under the governing
rules, and unchanged source authority with `authority_effect: none`.

This selector authorizes planning documentation and review evidence only. It
does not implement Projection behavior, close `PG-PROJ-01` or `PG-PROJ-02`,
claim Phase-3 completion, create an implementation packet, or start another
slice.

## Frozen causal outcome registry

| Integrated outcome ID | Packet IDs | Authority reference |
|---|---|---|
| `hcm-3.3-pg-proj-01-multi-envelope-proof-remediation` | `HCM-3.3-P2-pg-proj-01-proof-remediation-causal-review` | this corrective selector |

The registry is frozen before discovery. Every dispatch for this parent must
carry the same canonical registry fingerprint and the causal budget derived
from this parent orchestration ID plus the integrated outcome ID.

## Review subject and cadence

Discovery receives the complete pre-repair subject: this selector, the live
HCM-3.3 `SPEC.md`, and `tasks/plan.md`. It receives the exact `PG-PROJ-01`
gate, canonical Projection definition/request/result authority, and hard
non-goals, but no prior conclusion or success claim. Review is read-only,
fresh, isolated, and findings-first.

Every valid P1/P2 is consolidated into one parent-owned remediation, followed
by one different-fresh delta-focused closure. Only a P1/P2 directly caused or
unmasked by that repair may consume one of at most two supplemental causal
cycles. General discovery cannot reopen during closure, no review may follow
CLEAN, and no identity rename may reset the budget.

## Preflight evidence

- assigned checkout:
  `C:/Users/spmcc/.codex/worktrees/1ddb/handbook`;
- detached `HEAD` and dedicated local integration ref:
  `b35aaa1dd63d78901b6ef468bee55a22a3874b73`;
- exact base tree:
  `2db37cbff5615f8563a510701da531876c69ee06`;
- required ancestor:
  `12c203f605c26ce7d1911e4ee60c0de205822000` (verified);
- worktree state before this selector: clean;
- configured remote: `origin`; declared observation-only baseline:
  `12c203f605c26ce7d1911e4ee60c0de205822000`; no network access performed;
- protected product-checkout observation:
  `sha256:8d23e3d7e02dc4b95cfa7f6d092d7184c43b476531edbecfdc2d32eefd13f8ee`;
- protected meta-control-checkout observation:
  `sha256:f153c266da26601dce699382e615091b8a8d59497fa492c1b55aab7f15baef66`.

The target integration ref is not checked out by any worktree. The product and
meta-control checkouts are read-only observations for this run. Existing
untracked meta-control files are user-owned and remain untouched.

## Allowed primary and closeout scope

The material planning subject may change only:

1. `docs/specs/handbook-contract-membrane/slices/HCM-3.3/SPEC.md`;
2. `docs/specs/handbook-contract-membrane/slices/HCM-3.3/tasks/plan.md`;
3. the prior selector decision only if semantic coherence requires it; and
4. new HCM-3.3-local v1.4 dispatch/proof records for this corrective parent.

Mechanical closeout may add one new parent-owned v1.4 handoff and rebuild
`docs/specs/handbook-contract-membrane/handoffs/ledger.jsonl` deterministically.

## Hard non-goals and stop conditions

Do not modify Rust, tests, fixtures, profiles, schemas, registries,
validators, templates, skills, tools, dependencies, public APIs, versions,
release state, Snapshot Memory, HCM-0.11, HCM-3.2, HCM-3.4+, pipeline,
adopters, consumers, transports, old handoffs/dispatches, remote state, or any
protected checkout. Preserve reveal/derive-only semantics, no synthesis,
collapse without self-widening, and expansion only through an already
authorized broader request or typed escalation.

Stop non-completed if the required proof obligation cannot be repaired inside
this exact documentation subject, if a valid P1/P2 remains after the causal
allowance, if the target ref drifts, or if broader authority is required.
