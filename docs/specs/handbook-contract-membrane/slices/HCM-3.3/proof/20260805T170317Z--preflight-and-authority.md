# HCM-3.3 selector-plan preflight and authority proof

## Identity and repository binding

- Meta workflow: `handbook-hcm-3-3-selector-plan-20260805`.
- Dispatch nonce: `e4cbd8510bf44464be9351a9c82bc8f0`.
- Meta task/host: `019fd2d6-dc58-7562-90a9-37e6bbf711ee` / `local`.
- Increment task/host: `019fd2db-0721-74a0-9317-dc5e22c3ecc2` / `local`.
- Assigned mutable checkout: `C:/Users/spmcc/.codex/worktrees/8619/handbook`.
- HEAD, dedicated local integration ref, and required ancestor:
  `12c203f605c26ce7d1911e4ee60c0de205822000`.
- HEAD and dedicated ref tree:
  `d1cca0b342a8f671a4b9cef3f330cab42c0c32ef`.
- The checkout is detached, clean, and owned by this increment; the integration
  ref is not advanced during review.

## Local-only remote posture

`origin` is recorded only as the administrative remote name with baseline
`12c203f605c26ce7d1911e4ee60c0de205822000`. Per authorization, no fetch,
remote query, or push was performed; no live remote observation is claimed.
Final `landed.live_remote` will instead prove the live local target ref.

## Predecessor and authority admission

The selected HCM-3.2 record is schema-v1.4, `completed`, has
`stop_reason: completed`, reconciles eight completed built-in delegated runs,
and declares `PG-RES-01` closed only for its exact Context Resolution kernel.
Its automatic-continuation prohibition is superseded solely by this
planning-only user grant. It supplies immutable dependency context, not HCM-3.3
implementation authority.

The selected semantic/proof authority is limited to the HCM-3.3 Phase-map row,
the canonical Projection definition/request/result semantics, `PG-PROJ-01`,
`PG-PROJ-02`, and the live `07`/`08`/`09` orchestration protocol. No code
symbol is selected or modified, so upstream symbol impact analysis is not
applicable. GitNexus MCP capabilities are not exposed in this session; scoped
and compare-to-main detection will be reported as unavailable, never GREEN.

## Protected-path observations

The product checkout at `C:/Users/spmcc/Documents/__Project_Code/handbook`
has HEAD `1a4b10841d51803432bccd35348962e9e859c3b6`, tree
`8280353ac41a1431a32bee9ba6164226c0c17577`, and empty-status SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

The protected HCM-3.2/control-plane checkout at
`C:/Users/spmcc/.codex/worktrees/3b25/handbook` has HEAD
`12c203f605c26ce7d1911e4ee60c0de205822000`, tree
`d1cca0b342a8f671a4b9cef3f330cab42c0c32ef`, and status SHA-256
`325112ae87ed0bb479453f3ac6d4855751eb6dc426cc8e1102ad794668b82400`.
Its pre-existing untracked meta-orchestration files are preserved as
user-owned protected state and are outside this increment.

## Planning proof wall

Before each review dispatch, replay the complete planning manifest, inventory
all HCM-3.3-local selector/spec/plan/todo/proof paths, verify UTF-8 and
trailing-whitespace hygiene, run `git diff --check`, and validate the dispatch
with `validate_handoffs.py --verify-dispatch`. At closeout, run ordinary
validator and both self-tests, inspect exact changed paths, recheck protected
trees/status digests, and publish only through expected-old local
compare-and-swap.
