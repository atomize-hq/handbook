# HCM-2.4 supplemental causal remediation 2

Captured: `2026-07-26T04:57:51Z`

Predecessor causal closure: `/root/hcm24_planning_causal_closure_1`

Predecessor fingerprint:
`sha256:8b3e1b6a390314cda7a8c45e2ee7a954a99deb23db6c24942a214ef57026bcd5`

## Directly unmasked P2

The first supplemental remediation made the `artifact_instance.rs` guard an
explicit exact-row gate and proved Work Specification. The same repository-
reachable guard also applies to the planned Decision Record and Risk Record
repository profiles. Both P4 and P5 require descriptor-selected renderer proof,
but neither exact descriptor nor its guard admission had been frozen. Following
the plan could therefore leave renderer refs empty or fail with
`UnsupportedDependency`, violating the catalog-wide intake/renderer and
real-path proof outcome.

This is the same guard/descriptor causal family directly unmasked by
supplemental remediation 1. It is not general discovery.

## Consolidated final allowance remediation

The packet now freezes independent Decision Record and Risk Record fixture
roots, selection files, profile sources/refs, instance ids, kind refs, null
roles, fixed canonical paths, exact intake refs, singleton renderer refs,
`always` requiredness, and empty later-owned fields.

P1A now admits exactly five non-Charter descriptor rows:

1. Project Context;
2. Environment Context;
3. Work Specification;
4. Decision Record; and
5. Risk Record.

Every missing, extra, mismatched, or unrelated later-owned dependency remains
refused. P4/P5 separately prove the exact selected profile and canonical path.
Neither fixture alters the three-instance shipped-root profile, creates a
production default, infers a filename, adds a command, or authorizes generic
configured renderer/Projection behavior.

This consumes supplemental causal cycle 2, the final allowance. The next fresh
closure must be CLEAN for planning completion. Any remaining P1/P2 produces a
bounded non-completed stop rather than another remediation cycle.
