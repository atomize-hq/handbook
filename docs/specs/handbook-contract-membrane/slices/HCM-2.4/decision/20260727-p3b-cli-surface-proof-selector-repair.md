# HCM-2.4 P3B CLI surface proof selector repair

Status: **proposed exact test-only selector; implementation requires fresh
review**

Date: 2026-07-27

## Trigger

The post-P3 workspace wall ran:

```text
CARGO_TARGET_DIR=target/hcm-2-4-full-wall cargo test --workspace
```

It isolated eight failures in `handbook-cli --test cli_surface`. Every failure
belongs to the Stage 10 Work Specification cutover:

- standalone Stage 10 preview/apply fixtures still seed post-capture YAML and
  Markdown outputs;
- raw compile-payload assertions still expect the superseded
  `FEATURE_SPEC.md` refusal wording;
- full happy/skip journeys still test the pre-YAML authority/path wording; and
- the two committed journey transcripts still contain the superseded Stage 10
  payload, refusal, capture, and handoff bytes.

The production Stage 10 path is already P3 closure-review CLEAN. This is a
test/proof integration gap, not runtime authority to change that path.

## Exact P3B selector

Editable:

- `crates/cli/tests/cli_surface.rs`
- `tests/fixtures/foundation_flow_demo/evidence/happy_path.transcript.txt`
- `tests/fixtures/foundation_flow_demo/evidence/skip_path.transcript.txt`
- `docs/specs/handbook-contract-membrane/slices/HCM-2.4/proof/implementation/P3B-cli-surface-proof.md`

Within `cli_surface.rs`, edits are limited to:

1. Stage 10 fixture setup that removes only
   `artifacts/work-specification/work-specification.yaml` and
   `artifacts/feature_spec/FEATURE_SPEC.md` before capture;
2. the exact raw compile-payload refusal/recovery literals;
3. Stage 10 canonical-YAML and generated-view assertions; and
4. the existing happy/skip evidence-transcript comparisons needed to accept
   mechanically regenerated current output.

No other CLI behavior or test family is selected.

## Reviewability

P3B is atomic because the eight failures share one CLI integration file and
two byte-exact transcripts generated by those tests. It introduces no
production symbol, public API, dependency, Cargo/version, unsafe-policy,
schema, runtime, fixture profile, or sibling-slice change. The expected
hand-written delta is below the packet indicator; the transcript updates are
deterministic proof bytes.

## Entry and exit

Entry requires a fresh built-in review accepting this exact selector.

Exit requires:

- the eight formerly failing CLI tests pass;
- all 98 `cli_surface` tests pass;
- the P3 packet wall remains green;
- the complete workspace test wall is rerun;
- formatting, clippy, archive boundary, and diff checks pass; and
- a different fresh reviewer finds no unresolved P1/P2 in P3B.
