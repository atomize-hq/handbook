# HCM-3.4 Snapshot Memory planning selector decision

**Decision status:** frozen planning candidate pending one fresh discovery review and, only if valid P1/P2 findings require repair, a different-fresh closure review.

## Authority and selected outcome

This decision selects exactly one documentation-and-planning outcome: HCM-3.4-P1-snapshot-memory-planning-causal-review. The scoped planning-only operator grant authorizes this selector and packet despite the control pack's general later-slice deferral. It does not authorize product implementation, schema/API/dependency work, remote access, or publication.

Allowed reviewed primary-state paths are exactly:

- docs/specs/handbook-contract-membrane/slices/HCM-3.4/SPEC.md;
- docs/specs/handbook-contract-membrane/slices/HCM-3.4/tasks/plan.md;
- docs/specs/handbook-contract-membrane/slices/HCM-3.4/tasks/todo.md;
- this decision and HCM-3.4-local planning proof records; and
- HCM-3.4 v1.4 internal review dispatches strictly required to execute the planning review cadence.

The mechanical closeout may add only one parent-owned v1.4 handoff, its deterministically rebuilt ledger entry, and any exact P3/P4 inventory transcription. No product path is admitted.

## Dependency admission

The immediate completed predecessor is HCM-3.3 final record 20260805T230330Z--HCM-3-3--orchestration--protected-index-digest-contradiction. It is schema v1.4, completed, and records the final CLEAN closure of the private/internal deterministic Projection engine. It supplies the generic exact-source, fingerprint, omission, disclosure, and authority_effect: none boundary needed by a later HCM-3.5 snapshot-grounding consumer. Its completed status does not select or continue HCM-3.4; this decision is the sole current slice-selection authority.

HCM-3.2 remains immutable Context Resolution dependency evidence. The HCM-3.4 phase-map row, Snapshot Memory architecture/semantic/contract sections, and open PG-SNAP-01 through PG-SNAP-06 proof rows define the planned work.

## Selector decision

Plan one policy-driven deterministic Snapshot Memory capability with immutable normalized ContextMemorySnapshot records; derived stable/bounded/unstable consistency; separate state and record fingerprints; ordered compatible SnapshotDelta records; catalog-backed drift signals; fail-closed redaction, retention, and content-addressed deduplication posture; and paired prior-end/new-start boundary ordering. No model interprets observations, assigns drift meaning, or promotes an observation to authority.

The future capability is private/internal until a later explicit implementation selector proves otherwise. This planning decision deliberately selects no code symbol/path, schema, public DTO, default catalog, consumer, or transport.

## Causal outcome registry

| Integrated outcome ID | Packet ID | Authority reference |
|---|---|---|
| hcm-3.4-snapshot-memory-planning | HCM-3.4-P1-snapshot-memory-planning-causal-review | this decision |

The registry is frozen before discovery. Every HCM-3.4 planning review dispatch uses the same registry fingerprint and parent/outcome-derived causal budget.

## Review and proof posture

One fresh complete-subject discovery reviewer examines the selector, spec, plan, todo, and planning preflight. The parent validates findings under 09. It remediates all valid P1/P2 in one consolidated pass, replays the affected planning wall, and sends only that delta to one different fresh closure reviewer. At most two immediately causal supplemental cycles can address repair-caused/unmasked P1/P2 under unchanged scope. CLEAN ends review.

Planning proof is limited to exact local baseline/protected-path observations, predecessor and authority evidence, deterministic subject manifests, UTF-8 and whitespace checks, git diff --check, scoped inspection, v1.4 dispatch/handoff validation plus both self-tests, and expected-old local-only publication proof. GitNexus MCP capability is unavailable in this session, so its scoped and compare-to-main results must be recorded as unavailable rather than GREEN.

## Explicit non-goals

Do not implement, test, or claim Snapshot Memory behavior. Do not edit Rust, tests, fixtures, schemas, public APIs, dependencies, workflow/runtime configuration, completed HCM-3.3 evidence, HCM-3.5+, protected checkouts, local integration ref before closeout, or remote state. Do not fetch, query a remote, push, publish, rebase, merge, reset, or clean.
