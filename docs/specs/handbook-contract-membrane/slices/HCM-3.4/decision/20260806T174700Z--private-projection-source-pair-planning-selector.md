# HCM-3.4 private Projection source-pair planning selector

**Decision status:** frozen planning candidate pending one fresh complete-subject
discovery review and the required causal remediation/closure only if valid
P1/P2 findings are returned.

## Authority and selected outcome

This decision selects exactly one documentation-and-planning outcome:
`hcm-3.4-private-projection-source-pair-planning` /
`HCM-3.4-P2-private-projection-source-pair-planning`. It consumes the
immutable same-slice true stop
`20260806T064500Z--HCM-3-4--orchestration--snapshot-memory-projection-cardinality-stop`
under the explicit nonce-bound HCM-3.4 source-pair planning grant. The prior
record is source context, not an amended handoff, a continued implementation
parent, or a reset of its completed/blocked review budget.

Allowed reviewed-primary paths are limited to:

- `docs/specs/handbook-contract-membrane/slices/HCM-3.4/SPEC.md`;
- `docs/specs/handbook-contract-membrane/slices/HCM-3.4/tasks/plan.md`;
- `docs/specs/handbook-contract-membrane/slices/HCM-3.4/tasks/todo.md`;
- this selector and HCM-3.4-local source-pair planning proof records; and
- v1.4 internal planning-review dispatches required for this outcome.

Mechanical closeout may add only one parent-owned v1.4 handoff, its rebuilt
ledger entry, and a required exact P3/P4 inventory transcription. The work
does not authorize any implementation or generic Projection configuration.

## Dependency and baseline admission

`d7877f7843afbcda78d65e0fa2bf7093d3c71e6a` is the preserved pre-HCM-3.4
generic Projection baseline. HCM-3.3's completed v1.4 record is immutable
dependency context for exact source binding, currentness, typed omission,
hidden-data refusal, and `authority_effect: none`. HCM-3.4's blocked
projection-cardinality record establishes the additional private capability
needed by `PG-SNAP-04`: plural captured-family closure on one current snapshot
and one declared exact relation from its compatible delta.

The source-pair plan relies on the existing Snapshot-grounding Projection
definition in `05-contracts-schemas-and-gates.md`; it neither revises that
contract nor creates a runtime definition/configuration. HCM-3.5 adoption,
HCM-3.6 posture, public interfaces, and Phase-3 exit remain explicit
non-goals.

## Frozen remediation decision

Plan only an additive private generic source-pair mechanism with all of these
properties:

1. `snapshot_current` is exactly one immutable source with record/state
   identity and five canonical captured family tuples.
2. `snapshot_delta` is exactly one immutable source whose `to_snapshot`
   dependency equals that selected current record/state identity.
3. The generic engine validates declared pair identity before any currentness,
   disclosure, or payload access; snapshot/delta records retain ownership of
   endpoint compatibility.
4. Currentness derives from the bound current snapshot and an independent live
   observation for all five required families/slots, never from caller input or
   the delta.
5. Definitions without a pair requirement and legacy one-family sources retain
   their established exact behavior. Every missing, duplicate, malformed,
   incompatible, stale, substituted, or incomplete pair fails closed.
6. The only future proof is a fixed private test-only generic Projection
   integration fixture. It proves typed disclosure/omission, redaction without
   hidden reads, Resolution refusal, both-source provenance, and
   `authority_effect: none` without a consumer.

The planned raw source grammar is an exact XOR: legacy
`captured_revisions` or multi-family `captured_family_revisions`, never neither
or both. The normalized closure retains exact adapter ref/fingerprint pairs and
may use empty definition-declared slot sets. A pair-required current source
also carries private exact state identity through request and result provenance;
the selected delta's `to_snapshot` dependency must match both record and state
identity. Result currentness has exactly one canonical record per required
family, with work slot maps nested in its single record. These are private
model assertions, not a public or runtime configuration selection.

## Frozen outcome registry and causal budget

| Integrated outcome ID | Packet ID | Authority reference |
|---|---|---|
| `hcm-3.4-private-projection-source-pair-planning` | `HCM-3.4-P2-private-projection-source-pair-planning` | this selector |

This is a new planning outcome made necessary by the immutable blocked source
pair evidence, not a renamed implementation outcome. Its v1.4 registry and
parent/outcome-derived causal budget are frozen before its first review. It
permits one complete-subject discovery review, one consolidated P1/P2
remediation, and one different-fresh closure review, plus at most two strictly
causal supplemental cycles. No review may follow CLEAN.

## Proof and boundary posture

The planning proof wall is documentation-only: source/contract inspection,
exact worktree/ref/tree/ancestor/remote-baseline and clean-state observations,
manifest replay, UTF-8/whitespace and `git diff --check`, v1.4 dispatch and
handoff validation, both validator self-tests, local-only compare-and-swap
proof, and changed-path inspection. No product behavior test is run or
claimed. GitNexus MCP tooling and the local runner are unavailable in this
task environment; both scoped and compare-to-main detection are recorded as
unavailable, never GREEN.

Do not touch a protected checkout. The parent records their supplied preflight
manifests and proves this task staged/committed only assigned-worktree paths.
Do not query/fetch/push a remote, publish, merge, rebase, reset, or clean.
