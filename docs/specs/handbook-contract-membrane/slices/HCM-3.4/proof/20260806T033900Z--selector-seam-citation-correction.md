# HCM-3.4 selector seam-citation correction

This narrow supplemental correction resolves
`HCM34-I1-SEL-002` from the different-fresh selector closure. It does not
reopen discovery, amend either prior dispatch, change the frozen parent/outcome/
registry/budget, or modify the private implementation envelope.

The exact `03-seam-crosswalk.md` authority is its **Resolution-aware artifact
views** row, **Snapshot Memory** row, and **Context Resolution definition
stack** row. Together they establish the private engine/flow split, Snapshot
Memory's deterministic capture/delta/redaction/retention/Resolution-projection
boundary, and the completed HCM-3.2 Resolution prerequisite. The related
**Pipeline/Resolution semantics** section is contextual only and does not admit
pipeline work.

The exact `04-phase-slice-map.md` authority remains the `HCM-3.4` Snapshot
Memory row and `HCM-3.5` Resolution-aware snapshot/packet/pipeline adoption
row. HCM-3.5 is excluded consumer adoption. The exact `01` Snapshot Memory
posture and non-negotiable invariants, `02` semantics, `05` contracts, `06`
proof gates, and `07`/`08` selector/review/closeout protocols are unchanged.

No code is selected until this single supplemental causal review is CLEAN.
