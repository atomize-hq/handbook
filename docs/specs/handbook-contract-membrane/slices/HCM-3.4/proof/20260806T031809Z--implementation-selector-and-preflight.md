# HCM-3.4 Snapshot Memory implementation selector and preflight

## Dispatch identity and authority

- Parent orchestration: `20260806T031809Z--HCM-3-4--snapshot-memory-implementation`
- Increment task / host: `019fd50a-1e47-7a32-8ccd-3aaad8582e65` / `local`
- Meta workflow / thread / host: `handbook-hcm-3-4-implementation-20260806` / `019fd4ae-f9ee-7e82-bd09-499df96f9aa5` / `local`
- Dispatch nonce: `ae608ed25e00159bbd435b6a7b5bf1dd1f75cb285d8c6b07eb9c4864896e8fa2`
- Phase / slice / packet: `HCM-3` / `HCM-3.4` / `HCM-3.4-I1-snapshot-memory-implementation-causal-review`
- Selected handoff: `20260806T022000Z--HCM-3-4--orchestration--snapshot-memory-planning-protected-checkout-stop`
- Exact base / tree: `d7877f7843afbcda78d65e0fa2bf7093d3c71e6a` / `47440ba606d2fd1ccce340fbc9ff13553d3602d1`
- Dedicated local target: `refs/heads/orchestration/handbook-hcm-3-4-implementation-20260806`

The nonce-bound whole-slice implementation dispatch is the new operator
authority. The planning selector and its protected-checkout true stop are
immutable resume context, not an amendment target and not implementation
authority. This selector supersedes only their prospective implementation
prohibition; it does not reinterpret any planning evidence, modify a protected
checkout, or select HCM-3.5 or later work.

## Preflight evidence

The assigned checkout is detached at the exact base and tree above, is clean,
and owns its registered worktree entry. The dedicated local target equals the
same base and tree; the expected base is its required ancestor. The configured
remote is `origin`; the locally observed `origin/main` baseline remains
`a3babd20329027afacdcee9d8b7b9d638d15af5b`. No remote was queried, fetched,
or changed.

Protected checkouts were not entered or inspected by a command in this task.
The assigned checkout contains no uncommitted protected path and no staged
protected path. The planning true stop remains immutable evidence of its
separately adjudicated protected-index observation.

GitNexus was bootstrapped through the repository-authorized local CLI and is
current at this exact base. Full-text/BM25 search is unavailable because the
LadybugDB FTS extension is unavailable; it is not reported as GREEN. Exact
call-graph analysis is available. `execute_projection` is HIGH risk with 12
direct test callers and three affected test processes. This selector does not
modify that function or any existing Projection behavior. The HCM-3.4 private
Projection proof calls it through a new test only. Every existing symbol that
is later edited requires an exact fresh upstream impact report before that
edit.

## Selective context capsule

SLICE / OBJECTIVE: implement and prove one private deterministic Snapshot
Memory capability: policy-selected normalized immutable records, consistency,
paired boundaries, deterministic deltas/drift, fail-closed redaction and
retention, and private generic Projection evidence.

ACTIVE PACKET: `docs/specs/handbook-contract-membrane/slices/HCM-3.4/tasks/plan.md`.

DEPENDENCY / AUTHORIZATION PROOF: HCM-3.2 supplies immutable Context Resolution
evidence; HCM-3.3 supplies the completed private generic Projection engine;
the nonce-bound HCM-3.4 operator dispatch authorizes this implementation and
no sibling work.

SELECTED HANDOFF / VALIDITY: the exact HCM-3.4 planning true-stop record and
ledger entry agree. Its planning discovery P2s are remediated and its
different-fresh planning closure is CLEAN. Its status remains
`escalation_required` only for the former protected-checkout publication
boundary; this separately bound implementation increment neither resumes nor
repairs that parent.

ACTIVE RESOLUTION ENVELOPE: private engine, private configuration/fixtures,
unit and proof scope, and parent-local control artifacts only.

GROUNDING SNAPSHOT / START DELTA: Snapshot Memory is the selected capability;
no start snapshot or delta is fabricated before capture succeeds.

TARGET AUTHORITY BOUNDARY: internal `handbook-engine` implementation with no
public export, package version, dependency, default-catalog, consumer,
transport, SDK, CLI, pipeline, Handoff, or packet adoption.

CURRENT REPO-TRUTH STATUS: the completed generic Projection engine already
supports `snapshot` source-kind disclosure and exact captured-revision checks,
but no Snapshot Memory policy, record, delta, redaction, retention, or paired
boundary implementation exists.

MUST-READ PACK SECTIONS: HCM-3.4 `SPEC.md` and plan; HCM-3.4 in `04`;
Snapshot Memory and Projection semantics in `02`; Snapshot contracts in `05`;
PG-SNAP-01 through PG-SNAP-06 and regression rules 44-47 in `06`; live
`07`, `08`, and `09`.

LIVE SOURCE / TESTS / PRECEDENT: `crates/engine/src/lib.rs`, the private
`projection.rs` engine and its unit tests, the HCM-3.3 implementation selector,
and `definition_identity.rs` canonical fingerprint helpers.

SIBLING SEAMS IN CONTEXT: HCM-3.2 envelopes and HCM-3.3 Projection are
consumed without mutation. HCM-3.5 consumer adoption and HCM-3.6 posture use
remain excluded.

ALLOWED AREAS: the exact private paths below, this selector's proof/dispatch
artifacts, HCM-3.4 task ledger, and parent-owned closeout surfaces.

EXPLICIT NON-GOALS: public API, Cargo/dependency/version change, unsafe policy,
shipped defaults, generic Projection behavior changes, renderer/transport/SDK/
CLI/pipeline/Handoff/packet adoption, source mutation, model interpretation,
remote interaction, release, publication, or any other slice.

APPLICABLE CONTRACTS / PROOF GATES: exact policy/record/delta/redaction/
retention and generic Projection requirements plus PG-SNAP-01 through
PG-SNAP-06. No Phase-3 exit claim is available.

REQUIRED SKILL CHAIN: available equivalent to using-agent-skills,
context-engineering, documentation-and-ADRs, incremental-implementation,
test-driven-development, debugging-and-error-recovery on failure,
code-review-and-quality, git-workflow-and-versioning, and GitNexus impact/
change detection.

KNOWN CORRECTIONS OR CONFLICTS: the repository-local using-agent-skills path
named by `07` is absent; the available equivalent workflow is used. FTS concept
search is unavailable, but indexed call-graph impact and change detection are
available. The planning documents remain historical planning authority; this
selector supplies the new exact implementation boundary.

KNOWN P3/P4 ADVISORIES: `HCM-RF-0001` and `HCM-RF-0002` do not intersect this
private engine boundary.

MAXIMUM PERMITTED CLASSIFICATION / PROOF CHANGE: close only PG-SNAP-01 through
PG-SNAP-06 for the private capability. No public or consumer classification may
change.

EXIT PROOF: deterministic positive and fail-closed negative matrices, private
generic Projection proof with `authority_effect: none`, scoped crate proof,
GitNexus scoped/compare detection, different-fresh review closure, and a
two-commit v1.4 local-only closeout.

STOP CONDITIONS: required public/dependency/schema/transport/adopter scope,
an unreviewable risk expansion, unavailable mandatory proof, unresolved P1/P2
beyond cadence, integration-ref drift, protected-path change, or remote
interaction requirement.

## Frozen integrated outcome and causal budget

The sole integrated outcome is
`hcm-3.4-snapshot-memory-implementation`. Its sole packet is
`HCM-3.4-I1-snapshot-memory-implementation-causal-review`; this selector is
its authority reference. The registry is frozen before any implementation
review. The selector, implementation, proof, and final-closeout stages advance
monotonically under one parent/outcome-derived budget. Packet, selector,
fingerprint, or task renaming cannot create a new allowance.

## Exact owner, path, and packet envelope

Primary implementation may touch only:

1. `crates/engine/src/lib.rs` — add one private `mod snapshot_memory;`
   declaration. No public module/export, feature, version, or unsafe-policy
   change.
2. `crates/engine/src/snapshot_memory/mod.rs` — private module wiring and
   shared internal types only.
3. `crates/engine/src/snapshot_memory/policy.rs` — exact capture-policy,
   source-family/slot/window, comparison, predecessor, drift, and retention
   closure validation and policy fingerprinting.
4. `crates/engine/src/snapshot_memory/record.rs` — immutable normalized
   `ContextMemorySnapshot`, full family coverage/exclusion accounting,
   stable/bounded/unstable derivation, record/state fingerprint separation, and
   strict paired boundary predecessor selection.
5. `crates/engine/src/snapshot_memory/delta.rs` — ordered compatible
   `SnapshotDelta`, complete compare-or-exclude coverage, normalized changes,
   complete catalog evaluation, one-to-one signals, and durable justification
   requirements.
6. `crates/engine/src/snapshot_memory/redaction.rs` — fail-closed action and
   overlap evaluation, original/retained-pointer validation, exact
   horizon/trigger/record-class retention selection, and immutable storage
   optimization checks.
7. `crates/engine/src/snapshot_memory/projection.rs` — private conversion of
   an admissible snapshot/delta into exact generic Projection source bytes;
   no Snapshot-specific Projection engine or consumer adoption.
8. `crates/engine/src/snapshot_memory/tests.rs` and
   `crates/engine/src/snapshot_memory/fixtures/*.json` — deterministic
   PG-SNAP-01/02/03/05/06 positive and refusal tests and fixture data.
9. `crates/engine/src/projection/tests.rs` — add one private integration proof
   that feeds exact snapshot/delta source bytes through the unchanged generic
   Projection engine and proves all-family accounting, captured-revision
   currentness, typed omission/redaction without hidden reads, stale refusal,
   and `authority_effect: none`.

No existing function, method, type field, or generic Projection behavior is
selected for alteration. Any necessary deviation requires fresh impact analysis
and a selector repair followed by different-fresh review before that edit.

The seven internal packets run sequentially: (1) policy/source-family closure;
(2) normalized snapshot records; (3) consistency and paired boundaries; (4)
deltas/drift; (5) redaction/retention/deduplication; (6) private Projection
proof; (7) complete PG-SNAP proof closure. Packet edits remain internal child
work; packet-level work is never a top-level task.

## Positive, negative, and review wall

The implementation must establish PG-SNAP-01 through PG-SNAP-06 exactly as
specified. Its tests must cover stable identical-state replay and distinct
record identity; policy/window/family/slot/refusal; valid bounded and every
unstable/exclusion path; valid and invalid paired predecessor topology;
compatible and incompatible endpoint/delta behavior; complete ordered catalog
evaluation and signal bijection; durable justified divergence; all sensitive,
unknown, unmatched, matcher-failed, overlap, retained-pointer, hold/reference,
dedupe, and compaction paths; and generic Projection exact-source/currentness/
omission/hidden-read/stale/non-authority proof.

Before implementation code, a fresh isolated default reviewer must return a
schema-valid discovery result for this selector. Valid P1/P2 findings receive
one consolidated remediation and one different-fresh delta-focused closure.
No review follows CLEAN. Later implementation/proof reviews remain in this
same frozen parent/outcome budget and use fresh isolated built-in agents.
