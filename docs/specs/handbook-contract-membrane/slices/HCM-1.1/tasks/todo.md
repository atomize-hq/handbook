# HCM-1.1 Task Ledger

This ledger is future implementation work. HCM-0.7 approves the packet but does
not check off or execute these tasks. Canonical `07`/`08` owns orchestration,
review/remediation, true-stop, handoff, ledger, and two-commit mechanics.

## Entry and context

- [ ] Start a new top-level HCM-1.1 run with the exact completed HCM-0.7 handoff and this packet.
- [ ] Validate branch, HEAD, clean/attributable status, packet bytes, handoff/ledger parity, and Phase 0 dependencies.
- [ ] Reassemble the bounded authority/repo-truth/proof capsule from live files.
- [ ] Confirm the HCM-1.1 additive boundary and prohibited product-path/sibling files.
- [ ] Read and apply the packet's complete required skill chain.
- [ ] Refresh/check GitNexus; run upstream impact analysis before every existing symbol edit and warn on HIGH/CRITICAL risk.

## Task 1 — Exact identity and fingerprint primitives

- [ ] Write failing exact-ref, SemVer, fingerprint, JCS, duplicate-key, and limit tests.
- [ ] Add only the four pinned direct dependencies and inspect the lockfile diff.
- [ ] Implement `ExactDefinitionRef` and `DefinitionFingerprint` validation.
- [ ] Enforce the exact lowercase-ASCII identity segment/length grammar, canonical SemVer roundtrip, and single `@` delimiter with boundary negatives.
- [ ] Implement type-specific RFC 8785 normalization and SHA-256 derivation.
- [ ] Implement duplicate-key and fixed source-limit refusal.
- [ ] Implement closed typed decoding so unknown top-level/nested and wrong-record fields refuse before fingerprinting.
- [ ] Run focused tests, format, affected engine tests, scoped diff, and staged change detection.
- [ ] Commit the green atomic increment.

## Task 2 — Stable roles and trusted source loading

- [ ] Write failing frozen-fingerprint and unsafe-source tests.
- [ ] Add exact packaged core stable-role registry 1.0.0 and 1.1.0 assets.
- [ ] Implement typed stable-role parsing, validation, exact selection, and fingerprint replay.
- [ ] Refuse unknown registry/role fields at every nesting level before fingerprinting.
- [ ] Reuse the existing repo-relative/no-follow boundary with the smallest impact-reviewed visibility change.
- [ ] Prove missing, directory, symlink, traversal, duplicate-role, invalid-category, and substitution refusal.
- [ ] Prove both assets are included in package contents.
- [ ] Run focused/affected tests, format, scoped diff, and staged change detection.
- [ ] Commit the green atomic increment.

## Task 3 — Local Draft 2020-12 schema registry

- [ ] Write failing schema entry, local closure, meta-schema, and structural validation tests.
- [ ] Implement exact schema tuple/ref and entry/document/closure fingerprinting.
- [ ] Refuse unknown schema-entry fields and non-empty schema-entry extensions before fingerprinting.
- [ ] Implement explicit allowed roots and bounded local `$ref` prewalk.
- [ ] Refuse root/nested authored IDs, anchors, dynamic/recursive-reference keywords, and plain-name fragments under the frozen v1 resource policy.
- [ ] Require exact root Draft 2020-12 `$schema`; refuse missing/wrong/nested declarations and every schema-position keyword outside the frozen v1 allowlist.
- [ ] Prove schema-aware traversal does not treat object data under `const`, `enum`, `default`, or `examples` as keyword-bearing schema nodes.
- [ ] Bind each file to one exact internal base and prove prewalk, closure member, validator target, and reported schema location equality.
- [ ] Build only an in-memory Draft 2020-12 registry with resolver features disabled and linear regex.
- [ ] Return deterministic structural validation locations.
- [ ] Prove duplicate/conflicting identity and all remote/ambient/traversal/query/backslash/encoded/cycle/missing/limit refusal cases.
- [ ] Prove identifier rebasing, duplicate internal resource identity/alias, invalid JSON Pointer, and prewalk/validator target mismatch refusal.
- [ ] Assert the dependency graph contains no HTTP/TLS/async resolver path.
- [ ] Run focused/affected tests, format, scoped diff, and staged change detection.
- [ ] Commit the green atomic increment.

## Task 4 — Capability-free kind meta-validation

- [ ] Write failing exact capability-free kind and later-owned-field refusal tests.
- [ ] Implement `ArtifactKindDefinition`, registry load, lookup, and fingerprint closure.
- [ ] Enforce exact stable-role/schema rules without instance state.
- [ ] Refuse unknown top-level/nested kind fields, wrong-record fields, and non-empty kind extensions before fingerprinting.
- [ ] Prove kind records cannot carry instance path/label/requiredness/setup/intake authority.
- [ ] Refuse every non-empty semantic-capability, required-capability, semantic-validator, renderer, lifecycle, review-trigger, Projection, extension, or opaque dependency input.
- [ ] Prove forged digest, changed bytes, wrong dependency type, incompatible pointer-shape claim, and source-order substitution refuse before entering a fingerprint closure.
- [ ] Prove duplicate/conflict/missing/mismatch role/schema negatives.
- [ ] Run focused/affected tests, format, scoped diff, and staged change detection.
- [ ] Commit the green atomic increment.

## Task 5 — Repository custom-kind proof

- [ ] Write the failing public-API end-to-end custom-kind test.
- [ ] Add a primary capability-free exact kind plus one distinct proof-only companion, two schema entries/roots, a local-ref closure, and valid primary YAML fixture.
- [ ] Prove both accepted source permutations yield identical schema/kind registry fingerprints, exact lookup sets, closures, and validation behavior.
- [ ] Prove valid data passes and invalid data reports stable structural locations.
- [ ] Prove no `CanonicalArtifactKind` variant or sibling product-path/CLI change exists.
- [ ] Run complete engine tests, format, scoped diff, and staged change detection.
- [ ] Commit the green atomic increment.

## Full proof and review

- [ ] Run every focused positive case in SPEC.
- [ ] Run every negative/fail-closed/security case in SPEC.
- [ ] Run format, engine clippy/tests, full workspace tests, dependency-tree, package-content, archive, handoff-validator, and diff/scope proof.
- [ ] Update only the exact crosswalk and bounded PG-KIND-01/PG-KIND-02 structural evidence earned; leave both gates and all siblings open.
- [ ] Preserve a complete replayable HCM-1.1 proof wall.
- [ ] Assemble an immutable complete-subject review dispatch.
- [ ] Spawn a fresh isolated read-only built-in `default` reviewer and collect its structured result.
- [ ] Validate every finding against live authority.
- [ ] After any valid finding, remediate within scope, rerun the full proof wall, and use a different fresh reviewer.
- [ ] Require final CLEAN over the exact complete subject; do not mutate subject bytes afterward.

## Commit and parent closeout

- [ ] Replay the final clean manifest and full proof byte-identically.
- [ ] Stage only the reviewed HCM-1.1 subject and proof evidence.
- [ ] Run staged `git diff --check`, exact diff inspection, secret scan, and GitNexus change detection.
- [ ] Commit the reviewed HCM-1.1 implementation first.
- [ ] Create one completed v1.2 parent handoff bound to the primary commit and clean review lineage.
- [ ] Rebuild the ledger and run all validator/self-test modes.
- [ ] Stage only the mechanical handoff/ledger artifacts and run staged change detection.
- [ ] Commit the mechanical closeout separately.
- [ ] Stop without starting HCM-1.2.
