# HCM-3.6 planning source-impact and provenance proof

## Result

This is a documentation-only planning subject. It edits no product source,
function, class, method, product test, fixture, schema, configuration,
dependency, or generated artifact. Consequently no existing code symbol is
selected for modification and no code-symbol upstream impact analysis is
applicable. A future implementation selector must run upstream impact before
editing every existing symbol and must surface any HIGH/CRITICAL result before
work begins.

## Exact authority-to-planning map

| Authority | Current live truth retained | HCM-3.6 planning effect |
|---|---|---|
| `01` Charter/posture kernel | Charter is canonical; recommendation cannot self-enact. | Freeze owner and no-second-authority rule. |
| `02` posture kernel | Exact typed pairs, explicit freshness, global dimensions, advisory recommendation. | Freeze request, fingerprint, and evidence/refusal algebra. |
| `03` posture seam | `UsefulPrecursor`; no resolved kernel/recommendation/transition loop exists. | Do not claim adoption; plan only future `handbook-engine` ownership. |
| `04` HCM-3.6 row | Resolution/recommendation/transition/hysteresis/intake-reopen intent. | Bind implementation sequence and non-goals. |
| `05` posture contracts | Concrete trigger, basis, kernel, policy, recommendation, transition, and conformance rules. | Copy the future contract without creating a schema/API/runtime surface. |
| `06` PG-POSTURE-01/02 | Both gates are open. | Bind P01–P12 proof expectations; make no green claim. |
| HCM-3.4/HCM-3.5 records | Private source-pair and bounded grounding evidence are completed. | Allow only exact current redacted evidence-pair consumption in a future engine owner. |

## Repository and publication preflight

| Check | Observation | Result |
|---|---|---|
| Task checkout | `C:/Users/spmcc/.codex/worktrees/24cf/handbook` is the assigned detached worktree. | passed |
| Expected base / HEAD | `1256e724a2b7da6b6250f57d6f63fced1e2cf949` | passed |
| Expected tree | `ad5bac4904912e9a52b23ac862414deb79613154` | passed |
| Local integration ref | `refs/heads/orchestration/handbook-hcm-3-6-planning-20260807` equals expected base before edits. | passed |
| Required ancestry | expected base is ancestor of both HEAD and integration ref. | passed |
| Assigned worktree status | no staged, unstaged, or untracked path before the authorized work. | passed |
| Protected product checkout | clean at HEAD `1256e724a2b7da6b6250f57d6f63fced1e2cf949`, tree `ad5bac4904912e9a52b23ac862414deb79613154`. | passed |
| Protected HCM-3.6 meta checkout | clean at HEAD `e3d4a2530228c23e5a82106c2b51848895a25716`, tree `b01c805777b0cf5e758d58ec0e956324e361462b`. | passed |
| Remote observation | local tracking `origin/feat/handbook-contract-membrane` equals expected base; no fetch, pull, push, or other network action occurred. | passed |

GitNexus indexing was rebuilt locally with `npx --no-install gitnexus analyze`
because the index was absent. The command succeeded and changed only generated
AGENTS/CLAUDE index-count hunks, which were restored to expected-base bytes.
Its FTS extension is unavailable; any search-dependent output remains
unavailable, never GREEN. No source symbol changed, so an impact query is not
used to imply source impact proof.

## Future implementation source-impact ceiling

The next selected implementation packet must prove these before source edits:

1. `handbook-engine` contains or receives the one purpose-named
   `project_posture` owner; no existing Flow resolver, grounding path,
   HCM-3.4 private type, or HCM-3.5 view becomes posture owner.
2. Any canonical Charter read/write use is exact-reference/currentness guarded;
   only `apply_posture_transition` receives a constitutional mutation
   capability.
3. Snapshot and HCM-3.5 inputs are typed redacted evidence pairs, never raw
   snapshot/full delta payloads, hidden data, gate claims, or ownership paths.
4. New consumers are limited to typed recommendation/acknowledgement intent or
   outcome DTOs; adapter delivery cannot select policy/recipients/deadlines or
   approve a transition.
5. HCM-5 remains outside the call graph except as a future separately selected
   gate owner; no recommendation, handoff, packet result, or acknowledgement
   promotes a parent.

## Planning-surface impact

The only intended primary change set is the six paths frozen in the selector.
It adds no source symbol or executable flow. The future implementation impact
is deliberately described rather than enacted; thus this planning proof does
not assert tests, runtime behavior, a new module, or a changed classification.
