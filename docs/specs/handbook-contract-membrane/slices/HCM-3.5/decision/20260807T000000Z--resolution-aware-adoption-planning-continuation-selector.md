# HCM-3.5 Resolution-aware adoption planning continuation selector

**Status:** active documentation-only continuation selector. The required
read-only authority-admission selector is CLEAN. This selector freezes one
future adoption plan; it is not an implementation, publication, or completion
selector.

## Identity and authority

| Field | Value |
|---|---|
| Integrated outcome | `hcm-3-5-resolution-aware-adoption-planning` |
| Packet | `HCM-3.5-P1-resolution-aware-adoption-planning` |
| Parent orchestration | `20260806T202700Z--HCM-3-5--resolution-aware-adoption-planning` |
| Continuation grant | `hcm-3-5-resolution-aware-adoption-planning-continuation-v1` |
| Authority-admission dispatch | `20260807T000100Z--HCM-3-5--resolution-aware-adoption-planning-authority-admission-discovery` |
| Documentation implementation dispatch | `20260807T000200Z--HCM-3-5--resolution-aware-adoption-planning-documentation-implementation` |

The direct non-completed predecessor remains
`20260806T204100Z--HCM-3-5--orchestration--resolution-aware-adoption-planning-authority-boundary`.
Its original planning discovery and the different-parent authority attestation
remain immutable evidence; neither is amended, resumed, or relabeled.

## Selected documentation subject

The continuation subject is limited to this selector and its two local proof
records, `00` through `06` control-pack documentation, and the HCM-3.5
`SPEC.md`/`tasks/plan.md`/`tasks/todo.md` planning packet. It excludes the
original selector/preflight, `07`/`08`/`09`, dispatches, handoff records,
ledger, historical authority artifacts, product source, tests, configuration,
dependencies, package/registry state, remote state, staging, commits, and
refs.

## Frozen future adoption decision

1. `handbook-engine` is the typed owner of exact snapshot/delta binding,
   five-family currentness, redaction-before-read, bounded Resolution
   Projection, typed omission/provenance, and an engine-produced
   bounded/redacted delta-signal summary.
2. `handbook-flow` owns a separate greenfield packet-adoption path that
   consumes only the typed engine result. It does not extend the retained
   resolver contract, duplicate engine semantics, or promote a parent.
3. `handbook-pipeline` owns a separate greenfield namespaced shared-inclusion
   path. Raw `work_level`/L0–L3 may pass only through a declared one-way
   temporary mapping and never remains final authority or a refusal fallback.
4. HCM-3.4 `snapshot_delta` stays relation-only. The summary, not raw
   `/signals`, is reconciled with `reveal_delta_signals`; Flow and pipeline do
   not receive full delta signals, raw changes, or snapshot payloads.
5. Parent transitions use exact descriptive prior-end/start/grounding/end/delta
   references. Local completion and parent-promotion eligibility remain
   separate non-promoting evidence dimensions until the selected gate owner
   evaluates them.
6. The future standalone Handbook CLI adapts SDK composition. Substrate
   consumes or wraps exact published crates.io SDK/library versions; Tier 2
   exact binary/JSON remains an isolated transitional bridge.

These are architectural and proof decisions only. This selector deliberately
does not name a Rust signature, public type/DTO, schema, package version,
operation ID, implementation path, consumer migration, publication step, or
gate-runtime implementation.

## Required fail-closed posture

Missing, stale, incompatible, malformed, duplicate, uncurrent, insufficient,
redacted-only, unsupported, overbroad, or indeterminate input must produce a
typed refusal, omission, unavailable state, or non-promotion. No byte budget,
raw work level, handoff, packet-ready result, advisory score, or local success
substitutes for Resolution, currentness, or gate evaluation.

GitNexus MCP/CLI/index is unavailable in this checkout. Any required future
impact/change result is therefore recorded as unavailable, never GREEN.

## Review and stop conditions

The next review is one fresh, complete-subject, independent documentation
discovery review under the inherited same-parent causal budget. A valid P1/P2
may use only the direct causal remediation/closure allowance. No review follows
CLEAN. This child creates no global closeout state and cannot claim the parent
slice completed.

Stop rather than broaden for a Rust/runtime/public-API/schema/configuration/
dependency/SDK/CLI/Substrate/consumer/publication/registry/remote need, a
gate-runtime decision, protected historical path, or any contradiction that
cannot be resolved inside this documentation ceiling.
