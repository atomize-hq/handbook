# HCM-3.5 Resolution-aware adoption planning ledger

## Completed documentation decisions

- [x] Consume immutable HCM-3.4 private Snapshot Memory/source-pair evidence
  as a dependency without changing its history, visibility, or semantics.
- [x] Freeze the future `handbook-engine` -> `handbook-flow` ->
  `handbook-pipeline` typed owner chain, acyclic dependency direction, and
  greenfield purpose-named cutover posture.
- [x] Freeze relation-only `snapshot_delta` and engine-only bounded/redacted
  delta-signal summary semantics reconciled with `reveal_delta_signals`.
- [x] Freeze namespaced shared pipeline inclusion, exact one-way temporary
  legacy mapping rules, and no raw-level fallback after a typed-path refusal.
- [x] Freeze session prior-end/start/grounding/end/delta reference ordering and
  the descriptive/non-authoritative handoff boundary.
- [x] Freeze local-completion versus parent-promotion evidence as typed,
  separately fingerprinted, default-false-or-unavailable, non-promoting data.
- [x] Freeze future standalone CLI-over-SDK and exact crates.io
  SDK/library-to-Substrate composition, with Tier 2 binary/JSON remaining
  transitional and separately proved.

## Required documentation review work

- [x] Record HCM-3.5-P0's exact `handbook_engine::grounding` operation/value
  boundary, refusal algebra, delta-summary partition, non-promoting evidence,
  and Packet 1 source/test/proof revalidation ceiling.
- [ ] Review the complete HCM-3.5 documentation subject with a fresh,
  independent discovery reviewer under the reserved same-parent continuation
  cadence.
- [ ] Remediate only a valid P1/P2 inside this documentation ceiling, then
  obtain a different-fresh closure review. Do not run a review after CLEAN.
- [ ] Keep any P3/P4 disposition out of this child task; the parent alone may
  handle the permitted inventory/closeout mechanics.

## Future implementation checklist (not authorized by this packet)

- [ ] Select concrete code/API details in a new implementation selector and
  run fresh impact analysis before every symbol edit.
- [ ] Implement engine grounding with exact source pair/currentness,
  redaction-before-read, bounded Resolution Projection, typed omissions, and
  bounded/redacted signal summary proof.
- [ ] Implement the separate Flow packet path without changing the retained
  resolver contract or reimplementing engine semantics.
- [ ] Implement the separate pipeline namespaced shared-inclusion path and
  prove no raw `work_level` fallback.
- [ ] Populate session transition refs only after exact capture/currentness
  validation and prove every partial/unstable/stale/incompatible refusal.
- [ ] Integrate the non-promoting evidence boundary only when the selected
  gate-runtime owner exists; prove local completion cannot promote a parent.
- [ ] Complete the separately authorized SDK/standalone CLI/publication and
  current-tip Substrate real-seam proof before claiming permanent adoption.

## Explicit non-goals

- [ ] No Rust, tests, fixtures, public API, schema, configuration, dependency,
  SDK/CLI/Substrate, package, registry, remote, or consumer implementation.
- [ ] No HCM-3.4 rewrite, raw full-delta signal route, gate-runtime creation,
  parent-promotion result, fabricated snapshot ref, or adoption claim.
- [ ] No historical selector/preflight, dispatch, handoff record/ledger,
  HCM-3.6/HCM-4+/HCM-5, or ref update. Packet 0 alone may create its reviewed
  documentation dispatch, handoff/ledger records, and local closeout commits.
