# HCM-3.1 P1/P2 Implementation Convergence

Recorded at: 2026-08-01T13:25:27Z

Reviewed implementation candidates:

- `bbe3140` — typed vocabulary semantics and exact profile identity;
- `3be3200` — fixed Work Specification renderer adoption and real-path proof.

## RED evidence

`cargo test -p handbook-engine --test vocabulary_registry` first failed with the
missing `VocabularyResolution` type and missing typed label, role, alias, and
absorption methods.

`cargo test -p handbook-pipeline --test pipeline_capture
capture_apply_stage_10_consumes_selected_vocabulary_without_absorption_loss`
then reached the selected HCM-3.1 repository profile and failed because the
existing fixed renderer neither accepted the selected profile identity nor
rendered vocabulary. The fixture was corrected to a legal single repository
leaf before implementation; no production behavior was used to mask fixture
invalidity.

## GREEN evidence

| Command | Result |
|---|---|
| `cargo test -p handbook-engine --test vocabulary_registry` | PASS — 7/7 |
| `cargo test -p handbook-engine --test profile_selection` | PASS — 20/20 |
| `cargo test -p handbook-pipeline --test pipeline_capture` | PASS — 48/48 |
| `cargo test -p handbook-compiler --test pipeline_capture` | PASS — 39/39 |
| `cargo test --workspace --all-targets --all-features` | PASS |
| `cargo fmt --all -- --check` | PASS |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | PASS |
| `git diff --check 8004da7..HEAD` | PASS |

The full workspace suite included the engine, flow, pipeline, compiler, and CLI
targets. Two existing CLI tests legitimately ran for more than 60 seconds and
completed successfully; no test was skipped or terminated.

The implementation-only forbidden-influence scan over `8004da7..HEAD` returned
no match for newly implemented Context Resolution envelopes/execution,
capitalized Projection creation, Snapshot, posture, public Tauri/SDK/plugin, or
unsafe integration. No Cargo manifest changed.

## Exact proof outcomes

- The immutable shipped vocabulary replays
  `sha256:69113b1a9271ce207d45bdb91ebae8d6516249e16b59891b292546078364a22b`.
- The non-empty test vocabulary replays
  `sha256:0a28353460ce60a0fc53ba5e99ea2ec753abf94638489fe1eebd69b317d90ec4`
  independent of authored alias case and list order.
- Missing labels use stable-role 1.1 canonical labels; explicit labels remain
  presentation-only; duplicate displayed labels produce sorted ambiguity while
  typed role lookup remains direct.
- Unknown/capability roles, constitutional/non-workflow absorption, duplicate
  ownership/targets, undeclared extensions, and directed cycles refuse with
  typed error kinds.
- The selected profile retains the exact vocabulary identity/fingerprint and
  changes resolved-profile identity without changing the artifact-instance
  registry fingerprint.
- The shipped empty vocabulary preserves the existing Work Specification golden.
- The non-empty real Stage-10 capture keeps canonical Work Specification YAML
  byte-identical and replays the Markdown golden twice. It renders two unit
  declarations and all three directed absorption edges with stable role IDs.

## GitNexus evidence

Pre-edit upstream impact returned exact LOW risk, zero indexed direct callers,
zero indexed processes, and zero indexed modules for the `VocabularyDefinition`,
`AuthoredVocabulary`, and `ArtifactRepositoryV1` impl/struct symbols. The local
index did not expose the Rust functions `resolve_profile_selection`,
`admit_work_specification`, or `render_work_specification_markdown`; those
individual impact results were UNKNOWN and were bounded by the CLEAN selector
and exact source-call trace before editing.

The first scoped change-detection run reported LOW risk and zero affected
processes for the vocabulary symbols, though it also surfaced stale parser
artifacts. Subsequent scoped and compare-to-`main` invocations exited without a
change report while FTS/index support was degraded. Those modes are recorded as
unavailable, not GREEN.

## Protected state

All nine protected paths matched their initial SHA-256 values before both local
implementation commits and remained unstaged and excluded from each commit.
