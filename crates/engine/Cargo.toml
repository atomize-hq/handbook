[package]
name = "handbook-engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "handbook_engine"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
